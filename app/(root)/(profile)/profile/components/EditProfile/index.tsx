import Popup from "@/components/Popups/BasePopup";

export function EditProfile() {
  return (
    <Popup setShowState={setShowState} showState={showState} title="Editar Perfil" description="Personalize seu perfil e deixe ele atrativo para outros usuários">

    </Popup>
  )
}
